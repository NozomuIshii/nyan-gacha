import React from "react";
import ReactDOM from "react-dom/client";
import NyanGachaSimulator from "./NyanGachaSimulator.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NyanGachaSimulator />
  </React.StrictMode>
);
